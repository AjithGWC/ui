import React from 'react';
import Header from './header';
import Cards from './cards';

const index = () => {
    return (
        <div>
          <Header />
          <Cards />
        </div>
      );
}

export default index;